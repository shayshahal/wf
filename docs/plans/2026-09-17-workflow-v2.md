> Source: `show-me-jewelryx-workflow-plan.html` (rev 4) and `research-*.md` beside it, outside the repo. This file is the executable plan.

<div id="rule" class="section">

*00 / Two rules*

## Files + git + one CLI are the process. Everything else is optional.

    contract   <worktree>/.wf/state.json · SPEC.md · REVIEW.md · verification/decisions/<feature>/*.md (ADR · PDR · DDR)
               .gitattributes · AGENTS.md · JewelryX-Tools/process/*.md · lefthook · CI
    wf CLI     classify · step · status · decisions build · archive-round        ~250 lines
    adapters   herdr · pi / claude skills (3-line pointers) · plannotator · anything on the trigger list
               detected at runtime · ≤ 20 lines · removable

**TEST 1 · bare mode**

One Class-A round with Codex or Claude Code in a plain terminal — no Herdr, no pi, no Plannotator, no superpowers — from `AGENTS.md` + `process/` + `pnpm wf`. Runs in Task 3 and after Task 17.

**TEST 2 · overengineering**

If "what did we build?" no longer fits on one screen, we grew. This page must stay shorter than rev 3. No tool leaves the trigger table without its trigger having fired.

*01 / Target*

## The round

    classify          .gitattributes wf-class → A routine · B contract-touching · C product-undecided     wf step classify
    design (B/C)      Shay + one agent, one pane · usage → types → call stack → tests-to-write · ≥2 candidates · 15 min
                      SPEC.md written when Shay says "shared" · sha256                                     wf step design → implement
    implement         worker against SPEC · never edits verification/ · anti-slop + import-linter + ESLint rule
                      proof: red-base → green → red-reverted → green · walkthrough.webm · CALL-STACK-AS-BUILT.md
    review (T2)       Shay in the worktree: editor diff · app running · comments → REVIEW.md               wf step review  ← YOU
    PR → dev, once    300–800 lines · CI · squash · approve = merge                                        wf step pr / merged
    after             one decisions entry · round folder archived (posted Monday copy kept) · Monday post
    daily (T3)        git log --merges --since=midnight → click through what merged · 20 min

**Touchpoints:** A = T2 · B = T1 + T2 · + the Hebrew Monday text, unchanged. Everything else that used to need approval (ACCEPTANCE, DESIGN, PLAN, dispositions) is agent-internal or gone.

*02 / The 18 tasks*

## In dependency order. Verify line = done.

Batch 1 (Tasks 1, 2, 4, 7, 8, 10) merged to `tools/workflow-v2` and reviewed on 17 Sep. Tasks 2b/2c added after the batch from two pain points: reviewing from the hub session, and worktrees without a server or an isolated DB. 2d added from the BJEW-600 pilot: an isolated DB that is empty moves setup into the round.

## Foundation (parallel, three worktrees)

### 1 · Triage via `.gitattributes`

*Create root `.gitattributes` entries · `wf/classify.mjs` (≤ 30 lines wrapping `git check-attr`) · `process/CLASSES.md`*

    packages/backend/app/models/**    wf-class=B      packages/backend/app/schemas/**   wf-class=B
    packages/backend/app/api/**       wf-class=B      packages/backend/openapi.json     wf-class=B
    packages/frontend/*/src/lib/*.remote.ts wf-class=B   packages/frontend/*/src/lib/server/** wf-class=B
    packages/frontend/shared/**       wf-class=B      **/auth/** **/permissions* **/core/security* wf-class=B
    package.json pnpm-lock.yaml pyproject.toml requirements.txt wf-class=B
    **                                wf-class=A      # last match wins; no brace expansion; * does not cross /
    git diff --name-only dev...HEAD | git check-attr --stdin wf-class      # any B ⇒ B; C is set by the orchestrator, never by paths

**Verify:** TJEW-661 range (`1117c35ad..e4263bd3a`) → B via `openapi.json`; a Svelte-only diff → A.

### 2 · `wf step` / `wf status`

*Create `wf/wf.mjs` · `wf/step.mjs` · `wf/status.mjs` · `wf/adapters/herdr.mjs` · `.wf/` in `.gitignore` · root script `"wf"` · one selfcheck (this is the thing others depend on)*

    .wf/state.json   { round, class, step, waiting_on, since }     steps: classify design implement review pr merged held
    wf status        git worktree list → state.json each + gh pr list --json headRefName,number,isDraft,reviewDecision,statusCheckRollup
                     + wf decisions open --json → one table, waiting_on=shay first, red · --json
    adapters/herdr   present() = HERDR_ENV==='1'; on step → herdr pane report-metadata --token summary="review ← YOU"   (optional: herdr integration install pi)

**Verify:** three worktrees at different steps, `HERDR_ENV` unset → three rows, review first. With Herdr → same in the sidebar.

### 2b · `wf design` / `wf review` — both human touchpoints from the hub session **D11 · D12**

*Create `wf/design.mjs` · `wf/review.mjs` · `wf/adapters/plannotator.mjs` · `process/REVIEW-FORMAT.md` (moves here from Task 5) · after Task 2, before Task 3. Contracts: `<worktree>/SPEC-REVIEW.md` (T1) and `<worktree>/REVIEW.md` (T2), both `file:line — text` + a verdict line.*

    wf design <round>      resolves the worktree · opens SPEC.md for annotation · folds the result → SPEC-REVIEW.md · wf step design --waiting-on shay
    wf review <round>      resolves the worktree · server up (Task 2c) · REVIEW.md skeleton: URLs · files vs dev · class · spec sha
                           opens the diff for annotation · folds the result → REVIEW.md · wf step review --waiting-on shay
    wf review <round> --done   step implement (has lines) | pr (approved)
    adapters/plannotator   present(): binary on PATH. Spiked 17 Sep (v0.27.15): stdout carries only {decision, message}; the structured annotations[]
                           land in {PLANNOTATOR_DATA_DIR}/feedback/<project>/index.jsonl on submit. So both commands: spawn blocking with cwd=<worktree>,
                           PLANNOTATOR_DATA_DIR=<worktree>/.wf/plannotator, then fold the newest line (ts ≥ spawn, surface annotate|review) → the .md
                           T1: plannotator annotate C:/…/SPEC.md --gate --json --require-approval     T2: plannotator review --base dev --diff-type branch --json
                           Windows: C:/ paths only (the exe rejects /c/… and /tmp)
    fallback (no adapter)  $EDITOR, else `code`, else print the path — opens the worktree + the skeleton; you write the lines by hand
    agent side             DESIGN-SESSION.md: "read SPEC-REVIEW.md before revising" · worker: "read REVIEW.md before continuing"

**Verify:** from the hub session, annotate a SPEC in worktree X and review a diff in worktree Y — both land as files in the right worktree, no folder switch; the same two commands with Plannotator uninstalled fall back to \$EDITOR and still produce the files.

### 2c · Worktree lifecycle: server up on create · DB per worktree · reap only after T2 **D13**

*Modify `.config/wt.toml` · `scripts/sanitize-worktree-env.mjs` · `scripts/dev-worktree.mjs` · Create `docker-compose.worktree.yml` · `scripts/worktree-db.mjs` · `wf/status.mjs` column. Measured today: only `wt switch` runs post-start, so worktrees made with `git worktree add` have no server (workflow-v2 and dev were both down); every worktree shares dev's Atlas `DATABASE_NAME`.*

    create      wf creates worktrees via `wt switch --create` (hooks run) — never raw `git worktree add`
    server      post-start tether unchanged · wf status column  b2b :15748 ✓|✗  (1 s HEAD probe) · wf review restarts a dead tether
    db          per worktree: mongo container on a hashed port (docker-compose.worktree.yml, name jewelryx-mongo-<slug>)
                sanitize-worktree-env.mjs rewrites MONGODB_URL → localhost:<port>, DATABASE_NAME → jewelryx_<slug>
                seed: init_db() + seed_fixed_categories + SEED_ADMIN=true, then the Playwright fixtures' own setup   (measure: must be < 60 s)
    reap        wt.toml pre-remove hook: refuse unless .wf/state.json step == merged (or --force) · container removed in post-remove
                wf archive-round is the only normal caller of wt remove

**Verify:** `wt switch --create probe/x` → three servers answer on their ports and a fresh mongo answers on its port with categories seeded, ≤ 60 s; `wt remove` refused at step review, allowed at merged, container gone afterwards.

### 2d · Deterministic seed + snapshot restore — setup out of the round's hour

*Create `packages/backend/scripts/seed_worktree.py` · `docs/agents/seed.md` · Modify `scripts/worktree-db.mjs` (`seed` → restore-from-cache, `--reset`) · `wf.mjs` (`wf seed`) · `wf/new.mjs` (return after the tether, don't block on it). Measured on the BJEW-600 pilot (2026-09-19): the `db` hook seeds only categories + admin, so the round's first job was creating a B2B user and a gold request by hand; `seed_database.py` exists but is `random`-based, so no brief can name an id.*

    seeder      deterministic + idempotent, Latin fixture names (the suite's own rule), fixed ids/emails/passwords:
                admin · B2B seller · B2B buyer · ~10 products across categories · 1 stone with certificate
                1 marketplace listing · 1 auction with a bid · 1 scrap-gold request per status · 1 order per status · cash + loan balance
                (= the "populated stack" BASELINE.md JRN-06 names, plus its owners)   documented in docs/agents/seed.md so a brief can say "buyer@seed.jewelryx, SG-0001"
    snapshot    db hook: cache miss → run seeder, mongodump → ~/.cache/jewelryx-seed/<sha(seeder + app/models)>.archive
                cache hit → mongorestore only. Key changes with the seeder or a model ⇒ re-seed itself; cannot go stale
    reset       wf seed --reset   drop + restore mid-round when a repro dirtied the data
    keep        seed_database.py untouched (demo use); the hook stops calling the three-function snippet

**Verify:** fresh worktree DB ready **< 15 s** on a cache hit, < 60 s on a miss; `wf new` returns to the prompt with the three URLs and the seed credentials printed; a bug-fixer brief names a seeded buyer and request without the agent creating either.

### 3 · Class branch in the orchestrators + pilot TJEW-691 **D7**

*Modify both orchestrator SKILL.md · `_FLOW.md` · `_CR-FLOW.md` · Create `process/LIFECYCLE.md` (≤ 400 words: worktree → PR once → CI → squash) and `process/PRACTICES.md` (worktree-per-round, TDD, evidence-before-completion, one-subagent-per-task — a paragraph each) · `AGENTS.md` ≤ 100 lines as the index · `CLAUDE.md` = `@AGENTS.md`*

    A: no ACCEPTANCE/DESIGN/PLAN approvals — freeze capture, fix, prove, PR → T2
    B: design session (Task 5) → SPEC.md → worker → T2
    C: post the question AND continue on the decided remainder
    - "a round produces either a repair or a stop for a ruling"   + "ship what is decided, file what is not"

**Verify:** TJEW-691 («שליחת הצעה», Monday 3215597865) reaches a merged PR with zero approvals before the PR, ≤ 1 h. **Then bare mode:** a second Class-A bug with Codex or Claude Code, no Herdr/pi/Plannotator — passes or we fix the leak.

### 4 · Decision records: ADR · PDR · DDR — one file per decision, one schema

*Create `verification/decisions/_about.md` · `_schema.json` · `<feature>/` dirs (20) · `wf/decisions/parse.mjs` (YAML frontmatter + JSON-schema; the second selfcheck) · CI rule: a *modified* file under `decisions/` fails — change = new file with `supersedes:`*

    verification/decisions/cart/2026-09-09-TJEW-668-missing-product-skipped.md      one record, immutable once committed
    ---
    id: TJEW-668#2026-09-09-a      feature: cart      kind: PDR        # ADR engineering · PDR product · DDR design
    who: Einat                     date: 2026-09-09   status: accepted # proposed accepted rejected deprecated superseded (MADR)
    provenance: { monday: { board: 5088929904, item: 3179816127, update: 624745151 } }
    hash: sha256:3f9a…             # quote body, CRLF→LF, trailing ws stripped — else SUSPECT on Windows
    enforced: [cart-share.spec.ts#CART-071]        # the fitness function (ADR canon term)
    scope: [packages/backend/app/services/cart_service.py]   supersedes: null   review_trigger: null
    ---
    > «מוצר שהוסר מהאתר → מתעלמים»                 one line, never wrapped; rendered lang=he dir=rtl

    kind-specific body
    ADR  context / decision / consequences · alternatives:        provenance: PR, SPEC          enforced: import-linter contract, cell
    PDR  the verbatim quote · answers: · review_trigger:           provenance: monday item+update  enforced: Playwright cell
    DDR  authority: figma@<file>#<node> (b2b) | docs/design-system.md@<sha> + design-context.md@<sha> (admin) · deviates_from: + why
         written only for a deviation or a no-precedent screen (who: Einat); matching the authority = no record   enforced: pixel-parity capture (b2b) · reviewer at T2 (admin)
    open question = status: proposed + body "[NEEDS CLARIFICATION: … — waiting on X, asked …]"
    commit trailer: Decision: <id> sha256:<hash>      no merge=union needed — no shared file

**Verify:** selfcheck (one fixture per kind + one malformed) green · a second commit editing a record fails CI · `parse --check` validates every file against `_schema.json`.

## Design session (after Task 3)

### 5 · `process/DESIGN-SESSION.md` + formats

*Create `DESIGN-SESSION.md` (≈120 lines, harness-neutral; credits: pstack `/architect`, HumanLayer *Why Software Factories Fail* §program design, jsmastery-pro/skills `architect`/`develop`) · `SPEC-TEMPLATE.md` · `CALL-STACK-FORMAT.md` (backend + frontend variants) · `REVIEW-FORMAT.md` · 3-line pointer skills in `.pi/skills/design-session/`*

    1 prime  restate in ≤10 lines, ask nothing    2 ground  → SPEC ## As-is: current call stack + types per touched path (Shay checks this FIRST — a wrong line here costs thousands)
       + input-coverage test: every value the change produces has a named source, or it is an owed decision (B) / a question (C)    3 sketch  ≥2 structurally different:
      usage → types → signatures (bodies throw) → call stack → tests-to-write → one-line tradeoff  · falsifiers: any/cast · repeated workaround · escape-hatch param
    4 iterate  Shay edits types or writes SPEC-REVIEW.md lines · questions sorted INFER / ASK / RECOMMEND (never a menu, never silent)
    5 freeze  "shared" → SPEC.md → sha256 → wf step implement → stop      optional ## Slices: vertical cuts (contract+mock → screen → wire → store), wf step review after each
    call stack in diff syntax — the interesting part is what changes:
      entrypoint
        runCommand
     +    handleCreateResource          ← In → Out
     +      ResourceClient.create()     ✗ ErrorKind → lands: 302 / notice / skip / raise   (ruled | proposed | assumed)
     -    legacyCreateFlow
    file-tree diff:  + resource-client.ts NEW · ~ resource-route.ts MODIFIED     types/signatures block for every new function
    frontend: <Page> · state: union not booleans · <Child prop: T onEvent> · handler → x.remote.ts → invalidate · presentation-only = no stack, Class A

**Verify:** scratch run on closed TJEW-668: \## As-is first, ≥2 candidates, diff-syntax stack, every ✗ has a →, every produced value has a source; a 3-line REVIEW.md is acted on and deleted.

### 6 · Worker implements against SPEC + pilot TJEW-663 **D7**

*Modify `cr-implementer.md` · `bug-fixer.md`*

Read SPEC.md, verify sha; a needed signature change is stop-and-report. Deliver `proof/CALL-STACK-AS-BUILT.md` + diff vs SPEC, the four proof runs, `walkthrough.webm`. `wf step` at each transition.

**Verify:** TJEW-663 (inventory units between channels): T1 ≤ 20 min, SPEC ≤ 300 lines, T2 done in the worktree with the app running and a plain REVIEW.md.

## Linters (parallel with the above)

### 7 · Land `chore/migrate-to-oxvelte`, anti-slop at `error`

*Branch at `87c8056f1` · all `anti-slop/*` → error · oxlint = hygiene only (ESLint stays for structural rules)*

**Verify:** CI green; no `anti-slop/*` at warn.

### 8 · Python layers: import-linter **D2 · measured**

*Create `packages/backend/.importlinter` · dev dep `import-linter` (the one new dependency) · CI step `lint-imports`*

    layers = app.api | app.jobs → app.services → app.repositories → app.db
    unmatched_ignore_imports_alerting = error        # stale allowlist line fails CI — this is the ratchet
    broken_contract_guidance = Endpoints call services; services call repositories. See ARCHITECTURE.md § layers.
    ignore_imports = <the 10 measured edges>
    [leaves] forbidden · allow_indirect_imports = True · source app.schemas app.models app.core app.utils → forbidden api services repositories jobs

**Verify:** `lint-imports` exits 0 (0.36 s measured); a new endpoint importing `app.db` fails with the guidance line.

### 9 · TS seam rule in ESLint (not oxlint) **D3 · measured 0 findings**

*Modify `eslint.config.base.mjs`*

    no-restricted-syntax: CallExpression[callee.name='fetch'] > Literal[value=/^[/]api[/]/]
      message: "Call the backend through $lib/server/api-client.ts and *.remote.ts, never raw fetch. See ARCHITECTURE.md § data seam."
    overrides off for **/src/lib/server/**, **/*.remote.ts, **/hooks.server.ts        # oxlint lacks the rule and misses .svelte templates — measured

**Verify:** b2b 0, admin 0 (measured) · a component with `fetch('/api/x')` fails, template included.

### 10 · Oracle guard: lefthook job + CODEOWNERS + CI

*Modify `lefthook.yml` (do **not** set `core.hooksPath` — it would disable the existing commit-msg guard) · Create `.github/CODEOWNERS` · `.github/workflows/oracle-guard.yml`*

    pre-push: only: [{ref: fix/*}, {ref: feat/*}]  glob: "verification/**"  run: exit 1 with
      "product branches never edit the oracle; coverage lands via cr-to-tests / bugs-to-tests on a verification/* branch"
    CODEOWNERS  /verification/ @shay @saar   /JewelryX-Tools/ @shay @saar   /docs/design-system.md /docs/design-context.md @shay @saar   # admin design authority; edits are DDRs      CI: same rule for pushes that bypassed the hook

**Verify:** `fix/*` touching `verification/prd/` refused locally and in CI; `verification/*` branch passes.

### 11 · Delete the prose each mechanism now covers **D4**

*Create `PROHIBITIONS-MAP.md` · Modify the two flows*

148 must-not sentences → table: cover = anti-slop \| import-linter \| ESLint rule \| oracle-guard \| CODEOWNERS \| PRACTICES.md \| none. Delete the covered; Shay reads only the *none* rows.

**Verify:** counts before/after in the commit body.

## Decisions migration (after Task 4)

### 12 · Fold the nine ledgers into records, once

*Create `wf/decisions/fold-ledgers.mjs` (disposable; assertion inside: every quote's normalised sha equals its source, or abort) · `_unmapped.md` **D5***

sources/ + \_answers → PDR · \_dispositions + prd "Notes (date…)" → ADR · prd "Presentation (Figma …)" and "Deviation, deliberate" lines → DDR (PRDs shrink back to requirements) · held + open queue → status: proposed. Prefix table for route ids; TJEW/BJEW/MONDAY by grep; originals get a first-line `FOLDED … frozen` comment, deleted in Task 16.

**Verify:** entry count in the commit body · `_unmapped.md` ≤ 15 · `parse --check` green.

### 13 · `wf decisions build`: disposable SQLite → OPEN.md, held.json, README index, queries

*Create `wf/decisions/build.mjs` · `.wf/decisions.sqlite` (gitignored, rebuilt every run) · Modify `ask.mjs --answer` to write a record and rebuild · CI `--check` (schema · immutability · hash · `enforced` ids exist in `verification/tests` · `supersedes` targets exist)*

Every record → one SQLite row; `OPEN.md`, `README.md` (per feature, open first, newest first) and `held.json` are three queries rendered. `held.json` keeps the exact shape `ask.mjs / held.mjs / brief.mjs / next.mjs` destructure — ten gate readers unchanged. `wf decisions open --who Einat` · `for --path services/cart_service.py` · `show TJEW-668`; anything else is `sqlite3`. No HTML viewer. Escape hatch: if files ever become unbearable, stop regenerating the SQLite — the reverse migration never exists.

**Verify:** `ask.mjs --who Einat` identical before/after (diff in commit body).

### 14 · Orchestrators write decisions, not ledgers

*Modify cr-to-tests · PLAN-3-INTAKE · both orchestrators*

Intake → one PDR (verbatim, hash, provenance) and, for a Figma-backed change, a DDR. Design freeze → ≤ 3 ADRs. Class C → one `status: proposed` record with `asked:`. Intake asks one question: ADR, PDR or DDR — if "both", two records. No more `sources/*.source.md`, no hand edits to `queue.json`.

**Verify:** one dry intake ⇒ exactly one new entry.

## Human loop + cut (last)

### 15 · `ARCHITECTURE.md`, drafted by an agent, rewritten by Shay

*≤ 2 pages, matklad shape: codemap · the Task 8/9 contracts with the why · invariants as absences · entry points · names, no links. Agent picks what to cover from 6-month churn (`git log --since=6.months --name-only | sort | uniq -c | sort -rn | head -40`).*

**Verify:** Saar, cold, answers "where does a new B2B endpoint go and what may it import?"

### 16 · Archive: one-offs, folded ledgers, round folders on merge

*`git mv` the 20 dated `verification/*.md` → `verification/archive/2026/` · folded originals → `archive/ledgers/` · `wf archive-round` (≤ 30 lines): keep `MONDAY-he-*-posted.md`, move the rest. Walker re-runs only red-reverted (`--full` flag for suspicious rounds).*

**Verify:** `ls verification/*.md | wc -l` ≤ 20 · TJEW-661 folder → 1 kept + archive · all selfchecks green.

### 17 · Flows ≤ 2,000 words; bare-mode test again

*Rewrite `_FLOW.md` (10,122) · `_CR-FLOW.md` (7,124) · `bug-fixer.md` (2,929): purpose · class branch · one line per step naming its `wf step` · artifacts kept · "enforced by" links · cite `PRACTICES.md` instead of restating. Shay reads all three.*

**Verify:** `wc -w` ≤ 2,000 each · bare-mode Class-A round passes on the new text alone · count by hand, twice: verification:product commit ratio (was 15:2) and approvals per round (was 6+).

*03 / Not in the plan*

## Adopt only when the named pain appears

| Tool | Would replace | Trigger — do not touch before this happens |
|----|----|----|
| oasdiff · alembic check | reading the contract diff by eye at T2 | a contract change gets past T2 unnoticed |
| ESLint bulk suppressions · dependency-cruiser | hand allowlists · mermaid for ARCHITECTURE.md | a boundary rule has a non-zero allowlist (today: 0) |
| StrykerJS `--mutate file:lines --inPlace` | the walker's manual revert (TS side) | walker re-runs are the slowest step three rounds running |
| AppMap `sequence-diagram-diff` · codiff | hand-written CALL-STACK-AS-BUILT.md | as-built diffs are non-empty more often than not (and Python ≤3.12 / FastAPI ~0.110 fit) |
| Playwright `trace: 'on'` + recast · archify | walkthrough.webm · text call stacks | someone can't tell from the webm what happened · Shay wants the diagram |
| Midscene · Playwright Agents · `wf drive` | `git log --merges` and clicking | T3 is being skipped because 20 min is too long |
| graphify / vorpal · serena | grep · manual rename | rewriting ARCHITECTURE.md wants a map · D8's rename goes badly by hand |
| Stylelint / Tailwind class allowlist from design-system.md | reviewer's eye at T2 for admin tokens | an admin screen ships with an off-palette colour or off-scale type |
| Decision Guardian · ADR Guard | reading `scope:` by hand at T2 | a PR touches a path with a binding record and nobody noticed |
| HumanLayer (humanlayer.dev) | 2b + 2c + `wf status` — and, unwanted, layers 1–2 (state in their cloud, QRSPI's six phases, no pi, Windows via daemon+browser, \$100/user after 200 sessions/mo) | the \$EDITOR fallback hurts for five rounds AND you accept Claude Code/Codex, QRSPI ceremony and cloud sync of client code. Read, don't install: their open RPI/ACE-FCA material informs Task 5's DESIGN-SESSION.md. |
| entire · ank · orca · qlty · sonarqube · truecourse · GitNexus · codegraph · codebase-memory-mcp · chunkhound · bb · fallow (crashes on this repo) | — | never, unless the core stops working |
| portless — ADOPTED | raw hashed ports in worktree URLs | Shay, 2026-09-22 — named URLs for worktree stacks |

Research behind this table: `research-*.md` in this folder (12 files). Everything on it is a layer-3 adapter by construction; adopting one cannot change layers 1–2.

*04 / Decisions*

## All thirteen closed

**D1** Class-B paths incl. `frontend/shared/**` · **D2** `api|jobs → services → repositories → db`, 10 edges · **D3** ESLint no-raw-fetch, 0 findings · **D4** Shay reads *none* rows only · **D5** ≤ 15 unmapped · **D6** REVIEW.md is the contract · **D7** TJEW-691 (A), TJEW-663 (B), + a bare-mode A · **D8** rename `$lib/api → $lib/domain` after Task 9 · **D9** tool-agnostic · **D10** DDR authority per app — b2b: Figma; admin: `docs/design-system.md` + `design-context.md`; Einat rules deviations and no-precedent screens only · **D11** Plannotator is the first adapter for both T1 (annotate) and T2 (review); `$EDITOR` → `code` → print-path is the fallback · **D12** hub worktree: a checkout of dev never worked in, only orchestrated from — sessions start there, not at the bare root · **D13** DB per worktree = local MongoDB container on a hashed port, seeded by the backend's own seeders

*05 / Execution*

## On accept

- Export this page to `docs/superpowers/plans/2026-09-17-workflow-v2.md` on `tools/workflow-v2`.
- Tasks 1, 2, 4, 7, 8, 9, 10 in parallel (one subagent, one worktree each) → Shay T2 once for the batch. Then 3 (pilot) → 5, 6 (pilot) · 11–14 · 15–17.
- Commits carry `[wf2 N]`; `pnpm wf status` is the live view once Task 2 lands.
- Not doing: kanban, decision database, HTML viewer, stacked-PR tooling, changes to how Einat is asked, anything in the trigger table.

JewelryX / Workflow v2 · rev 4 · 17 tasks · Keep the words, freeze the expectation, measure the claim — and read the code.

